{% extends 'SyliusWebBundle:Backend:layout.html.twig' %}

{% import 'SyliusResourceBundle:Macros:buttons.html.twig' as buttons %}
{% from 'SyliusWebBundle:Backend/Macros:misc.html.twig' import pagination %}
{% from 'SyliusWebBundle:Backend/PaymentGateway:macros.html.twig' import list %}

{% block topbar %}
<ol class="breadcrumb">
    <li>{{ 'sylius.breadcrumb.configuration'|trans }}</li>
    <li>{{ 'sylius.breadcrumb.payment_gateway.index'|trans }}</a></li>
</ol>
{% endblock %}

{% block content %}
<div class="page-header">
    <div class="actions-menu">
        {{ buttons.create(path('sylius_backend_payment_gateway_create'), 'sylius.payment_gateway.create'|trans) }}
        {{ buttons.manage(path('sylius_backend_payment_index'), 'sylius.payment.manage'|trans) }}
    </div>
    <h1><i class="glyphicon glyphicon-credit-card"></i> {{ 'sylius.payment_gateway.index_header'|trans|raw }}</h1>
</div>

{{ pagination(payment_gateways) }}
{{ list(payment_gateways) }}
{{ pagination(payment_gateways) }}

{% endblock %}
